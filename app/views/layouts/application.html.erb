<!DOCTYPE html>
<html>
<head>
  <title>TODOList</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'TODOList by R1cro', root_path, class: "navbar-brand" %>
    </div>
    <ul class="nav navbar-nav">
      <% if current_user.present? %>
        <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete %></li>
        <% else %>
        <li><%= link_to 'Sign in', new_user_session_path%></li>
        <li><%= link_to 'New here?', new_user_registration_path%></li>
      <% end %>
    </ul>
    <p style="opacity: 0.3;" class="notice navbar-text navbar-center"><%= notice %></p>
    <p style="opacity: 0.3;" class="alert navbar-text navbar-center"><%= alert %></p>
    <% if user_signed_in? %>
        <%= link_to 'Create new task!', new_item_path, class: "navbar-text navbar-link navbar-right" %>
    <% end %>
  </div>
</nav>

<div class="container">
    <%= yield %>
</div>

</body>
</html>
