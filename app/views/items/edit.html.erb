<div class="col-md-6 col-md-offset-3">
    <% if @item.completed? %>
      <h3>WAT? How you...</h3>
      <h2>Anyway, your task is CLOSED.</h2>
      <h4>Now try remove your task.</h4>
      <br>
      <p><%= link_to @item, method: :delete do %></p>
           <i class="fa fa-lg fa-trash">Delete</i>
         <% end %>
    <% else %>
        <h2>Edit TODOList </h2>
        <%= render 'form' %>
    <% end %>
</div>